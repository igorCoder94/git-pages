import "./style.scss";
import data from "./data";
import ModuleBlock from "../../../shared/moduleBlock";

const Integration = () => {
  return (
    <section id="integration" className="integration__section">
      <ModuleBlock data={data} />
    </section>
  );
};

export default Integration;
